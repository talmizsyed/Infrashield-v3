{
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "lint": "eslint . --ext .ts",
    "test": "vitest run --config vitest.config.ts"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "name": "@infrashield/ai-routing",
  "dependencies": {
    "@infrashield/contracts": "workspace:*",
    "@infrashield/ai-core": "workspace:*",
    "@infrashield/core-infrastructure": "workspace:*"
  }
}
