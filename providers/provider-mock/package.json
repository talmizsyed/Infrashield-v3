{
  "name": "@infrashield/provider-mock",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "lint": "eslint . --ext .ts",
    "test": "vitest run"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@infrashield/contracts": "workspace:*",
    "@infrashield/providers": "workspace:*",
    "@infrashield/ai-tools": "workspace:*"
  }
}
